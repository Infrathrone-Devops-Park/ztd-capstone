API_GATEWAY_URL=http://api-gateway:8080
# Optional: override the DNS resolver nginx uses for /api/ proxying.
# Leave unset — the entrypoint auto-derives it from /etc/resolv.conf
# (Docker embedded DNS or k8s CoreDNS). Only set to force a specific address.
# RESOLVER_ADDR=127.0.0.11
